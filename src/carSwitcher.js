/*export class CarSwitcher {
    /* constructor(imageElementId) {
        this.imageElement = document.getElementById(imageElementId);
        this.currentIndex = 0;
        this.carImages = ['src/cle.png', 'src/m4Competetion.png', 'src/m4.png']; 
    }

    nextCar() {
        this.currentIndex = (this.currentIndex + 1) % this.carImages.length;
        console.log("Next Car Index: " + this.currentIndex);  // Debugging
        this.updateCarImage();
    }
    
    previousCar() {
        this.currentIndex = (this.currentIndex - 1 + this.carImages.length) % this.carImages.length;
        console.log("Previous Car Index: " + this.currentIndex);  // Debugging
        this.updateCarImage();
    }
    

    updateCarImage() {
        this.imageElement.src = this.carImages[this.currentIndex];
    } 
}*/
